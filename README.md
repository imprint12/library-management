book(ISBN, title, writer, publisher)

on_shelf_book(ISBN, price, number)

restore(ISBN, number, total_price)

payment_bill(no, date, total_price, arrived, paid)

collection_bill(no, date, total_price)

storage(ISBN, number)